If we have a radio input the syntax will be

<input
 type:"radio">  and we continue as normal but we have to have
 checked: {initial value from prop} === "to the specific value we want" 

 npx cypress open